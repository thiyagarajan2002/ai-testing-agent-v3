# Scripts

Scripts are grouped by operational purpose. CI helpers belong under `scripts/ci/`; future local development and release automation should use `scripts/dev/` and `scripts/release/` respectively.

Scripts must remain non-destructive by default and must not contain credentials.
