# Documentation Guide

This directory contains technical and operational documentation for AI Testing Agent.

## Structure

- `architecture/` — system and package architecture.
- `configuration/` — runtime configuration and environment guidance.
- `testing/` — test strategy, test data and validation guidance.
- `releases/` — changelog and release history.

`PROJECT_DETAILS.md` remains the complete project reference and must be updated for every version change.
