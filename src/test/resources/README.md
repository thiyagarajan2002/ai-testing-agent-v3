# Test Resources

Test-only fixtures belong here. Keep user-facing runnable examples under the repository `examples/` directory instead.

Recommended groups:

- `plans/`
- `suites/`
- `data/`
- `environments/`
